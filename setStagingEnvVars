#!/bin/bash
set -e

export GOOGLE_CLUSTER_NAME=${GOOGLE_CLUSTER_NAME_STG}
export GOOGLE_PROJECT_ID=${GOOGLE_PROJECT_ID_STG}
export GOOGLE_COMPUTE_ZONE=${GOOGLE_COMPUTE_ZONE_STG}
export GOOGLE_COMPUTE_REGION=${GOOGLE_COMPUTE_REGION_STG}
export GCP_SERVICE_ACCOUNT_KEY=${GCP_SERVICE_ACCOUNT_KEY_STG}
